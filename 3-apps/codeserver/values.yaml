persistence:
  enabled: true
  storageClass: "longhorn"
  accessMode: ReadWriteOnce
  size: 15Gi
  annotations: {}
  existingClaim: "code-server-pvc"
  # hostPath: /data
extraArgs:
  - --auth
  - none
