defaultNamespace: gotify

kustomize:
  dir: ./kustomize

helm:
  releaseName: "gotify"
  chart: "gotify"
  repo: "https://pmoscode-helm.github.io/gotify/"
  version: "0.4.0"
  valuesFrom:
    - secretKeyRef:
        name: gotify-secret-values
        namespace: gotify
        key: values.yaml
